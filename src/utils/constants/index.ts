import {
  responsiveHeight,
  responsiveFontSize,
  responsiveWidth,
} from 'react-native-responsive-dimensions';

export const hp = responsiveHeight,
  wp = responsiveWidth,
  fp = responsiveFontSize;
